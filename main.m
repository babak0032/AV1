
% Colours = cellstr(['r.'; 'b.'; 'g.'; 'c.'; 'r.'; 'r.']);

% A = all_observations;
% B = path;
% size(A);
% size(B);
%size(positions);

evaluation(all_observations, path)
%load('DATA1/positions1.mat')
% check_image(233, all_observations, positions)




